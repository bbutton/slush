#include <string.h>

int main(int argc, char** argv) {
  char * test_string = "this is my string";

  char * result = NULL;
  while((result = strtok(test_string, " ")) != NULL) {
    printf("%s\n", result);
  }
}
